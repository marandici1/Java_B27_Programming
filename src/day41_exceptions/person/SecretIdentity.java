package day41_exceptions.person;

class SecretIdentity extends Person {
    String name = "dnobsemaj";

    // doing variable hiding, the original one, is hidden, but now I have a new variable with the same name
}
